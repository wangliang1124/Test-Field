import Vue from 'vue';
import App from './index';

// import elAlert from './../../components/alert/index';

// Vue.component(elAlert.name, elAlert);
// Vue.use(elAlert)
// elAlert.install(Vue)

const app = new Vue(App);
app.$mount();
